# @package _global_

# Override entire configuration yaml files
defaults:
  - override /agent: rainbow
  - override /env: hockey

agent:
  name: rainbow_hockey_self_play
  requires_continues_action_space: True

  network:
    hidden_size: 256

  training:
    criterion: 
      reduction: 'none'
    episodes: 200
    batch_size: 128
    save_agent: True
    continue_training: True